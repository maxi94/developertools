import type { ToolDefinition } from '@/shared/types/tool'

export const tools: ToolDefinition[] = [
  {
    id: 'json-formatter',
    name: 'JSON Formatter',
    description: 'Formatea y valida JSON localmente, sin enviar datos.',
  },
]
